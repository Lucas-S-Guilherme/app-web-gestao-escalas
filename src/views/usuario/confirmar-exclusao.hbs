{{#> main}}
  {{#*inline "content"}}
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Confirmar Exclusão</h5>
            <p>Você tem certeza que deseja excluir o usuário <strong>{{usuario.nome_usuario}}</strong>?</p>
            <form method="POST" action="/usuario/{{usuario.id_usuario}}/exclusao">
              <button type="submit" class="btn btn-danger">Excluir</button>
              <a href="/usuario" class="btn btn-secondary">Cancelar</a>
            </form>
          </div>
        </div>
      </div>
    </div>
  {{/inline}}
{{/main}}