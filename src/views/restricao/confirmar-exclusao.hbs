{{#> main}}
  {{#*inline "content"}}
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Confirmar Exclusão</h5>
            <p>Você tem certeza que deseja excluir a restrição "{{restricao.nome_restricao}}"?</p>
            <form method="POST" action="/restricao/{{restricao.id_restricao}}/exclusao">
              <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
              <a href="/restricao" class="btn btn-secondary">Cancelar</a>
            </form>
          </div>
        </div>
      </div>
    </div>
  {{/inline}}
{{/main}}