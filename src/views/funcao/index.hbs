{{#> main}}
  {{#*inline "content"}}
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Lista de Funções</h5>
            {{#if successMessage}}
              <div class="alert alert-success">
                {{successMessage}}
              </div>
            {{/if}}
            {{#if errorMessage}}
              <div class="alert alert-danger">
                {{errorMessage}}
              </div>
            {{/if}}
            <a href="/funcao/novo" class="btn btn-primary mb-3">Nova Função</a>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>ID</th>
                    <th>Nome</th>
                    <th>Sigla</th>
                    <th>Ações</th>
                  </tr>
                </thead>
                <tbody>
                  {{#if funcoes}}
                    {{#each funcoes}}
                      <tr>
                        <td>{{this.id_funcao}}</td>
                        <td>{{this.nome_funcao}}</td>
                        <td>{{this.sigla_funcao}}</td>
                        <td>
                          <a href="/funcao/{{this.id_funcao}}/atualizacao" class="btn btn-sm btn-warning">Editar</a>
                          <a href="/funcao/{{this.id_funcao}}/exclusao" class="btn btn-sm btn-danger">Excluir</a>
                        </td>
                      </tr>
                    {{/each}}
                  {{else}}
                    <tr>
                      <td colspan="4" class="text-center">Nenhuma função encontrada.</td>
                    </tr>
                  {{/if}}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  {{/inline}}
{{/main}}